import { formatCurrency, formatCurrencySimple } from '../utils/financeCalculations';
import { motion } from 'framer-motion';

export default function Results({ results }) {
    if (!results) return null;

    return (
        <div className="w-full grid grid-cols-1 md:grid-cols-3 gap-6">
            {/* Required SIP Card — primary brand blue */}
            <motion.div
                initial={{ opacity: 0, y: 10 }}
                animate={{ opacity: 1, y: 0 }}
                whileHover={{ scale: 1.02, y: -4 }}
                transition={{ type: "spring", stiffness: 300 }}
                className="rounded-2xl p-6 sm:p-8 text-white relative overflow-hidden shadow-lg"
                style={{ background: '#224c87' }}
            >
                <div className="absolute top-0 right-0 w-48 h-48 rounded-full -translate-y-1/2 translate-x-1/3" style={{ background: 'rgba(255,255,255,0.06)' }} />
                <div className="relative z-10 flex flex-col gap-2">
                    <div className="text-xs font-semibold tracking-widest uppercase mb-2" style={{ color: 'rgba(255,255,255,0.75)' }}>
                        Required Monthly SIP
                    </div>
                    <h2 className="text-4xl sm:text-5xl font-bold tracking-tight tabular-nums">
                        {formatCurrency(results.sip)}
                    </h2>
                    <div className="mt-4 bg-white/10 p-4 rounded-xl border border-white/20">
                        <div className="text-xs uppercase tracking-wider mb-1" style={{ color: 'rgba(255,255,255,0.7)' }}>Inflation-Adjusted Target</div>
                        <div className="text-xl font-semibold tabular-nums">
                            {formatCurrencySimple(results.fv)}
                        </div>
                    </div>
                </div>
            </motion.div>

            {/* Total Capital Invested */}
            <motion.div
                initial={{ opacity: 0, y: 10 }}
                animate={{ opacity: 1, y: 0 }}
                whileHover={{ scale: 1.02, y: -4 }}
                transition={{ delay: 0.1, type: "spring", stiffness: 300 }}
                className="bg-white rounded-2xl p-6 shadow-sm border border-slate-200 flex flex-col justify-center cursor-default hover:shadow-md transition-all duration-300"
            >
                <div className="text-xs font-semibold uppercase tracking-widest mb-2" style={{ color: '#919090' }}>Total Capital Invested</div>
                <div className="text-2xl font-bold text-slate-800 tabular-nums">
                    {formatCurrencySimple(results.totalInvestment)}
                </div>
                <p className="text-xs mt-2" style={{ color: '#919090' }}>Sum of all monthly SIP contributions over the period.</p>
            </motion.div>

            {/* Compounded Returns */}
            <motion.div
                initial={{ opacity: 0, y: 10 }}
                animate={{ opacity: 1, y: 0 }}
                whileHover={{ scale: 1.02, y: -4 }}
                transition={{ delay: 0.2, type: "spring", stiffness: 300 }}
                className="bg-white rounded-2xl p-6 shadow-sm border border-slate-200 flex flex-col justify-center relative overflow-hidden cursor-default hover:shadow-md transition-all duration-300"
            >
                <div className="absolute top-0 right-0 w-24 h-24 rounded-full -translate-y-1/2 translate-x-1/3 pointer-events-none" style={{ background: 'rgba(34,76,135,0.05)' }} />
                <div className="relative z-10">
                    <div className="text-xs font-semibold uppercase tracking-widest mb-2" style={{ color: '#919090' }}>Compounded Returns</div>
                    <div className="text-2xl font-bold tabular-nums" style={{ color: '#224c87' }}>
                        + {formatCurrencySimple(results.wealthCreated)}
                    </div>
                    <p className="text-xs mt-2" style={{ color: '#919090' }}>Earnings generated by compounding over the investment period.</p>
                </div>
            </motion.div>

        </div>
    );
}
